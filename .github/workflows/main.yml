name: JUSTICE_RESKILL_COHORT_1
defaults:❤ 
 run:
    shell: 
    working-directory: 
jobs: 
  build: 
    name: Hello world action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: "Christopher Williams"
